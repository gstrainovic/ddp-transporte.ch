<div class="titel">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 os-animation" data-os-animation="zoomIn" data-os-animation-delay="0.3s">
                {% if page.header.title %}
                <h1 class="hr">{{ page.header.title }}</h1>
                {% endif %}
                {% if content %}
                <div class="top">{{ content }}</div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
